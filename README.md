# ioBroker.ems-optimizer

Observer-only adapter containing the EMS V0.2.9 history, forecast,
device-plan and ECharts engine. Adapter outputs are created below
`ems-optimizer.0`; no device actuator is written.
